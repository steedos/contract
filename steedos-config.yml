env:
  PORT: 5000
datasources:
  default:
    connection: 
      host: 127.0.0.1
      database: steedos
  mongo:
    connection: 
      url: mongodb://127.0.0.1/steedos
    objectFiles: 
      - "./src"
    appFiles:
      - "./src/contract.app.yml"
public:
  cfs:
    store: "local"
    local: 
      folder: "./storage"